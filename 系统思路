    # 读取数据库中的学生信息（学生信息表）
    # 登陆QQ邮箱，根据邮件内容，insert into 作业表，如果解析不出名字学号，insert into 垃圾表
    # 下载学生附件至download文件夹，解压至文件夹内，文件夹名为学生信息（如果存在则新建文件夹_2）
    # 读取该文件，bs4进行解析，计算dom，计算标签数、属性数，写入log
    # 将结果回复邮件